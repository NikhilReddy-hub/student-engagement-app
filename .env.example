# Database Configuration
# For production, use PostgreSQL connection string from your database provider
# Example: postgresql://user:password@host:5432/database?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/student_engagement_db?schema=public"

# JWT Secret
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-secret-key-change-in-production"

# Node Environment
NODE_ENV="development"

# Optional: For local development with SQLite (not recommended for production)
# DATABASE_URL="file:./dev.db"
